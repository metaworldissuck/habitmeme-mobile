# Web Layer

The mobile web UI is intentionally served directly by the FastAPI backend.

- templates live in `backend/templates`
- static assets live in `backend/static`

This `web/` directory exists to preserve the planned top-level structure for the final submission.

